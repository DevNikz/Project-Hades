public enum Rarity {
    Common,
    Uncommon,
    Rare,
    Epic,
    Legendary
}

public enum Kind {
    None
}

public enum AugmentType {
    AGGRO,
    STEEL
}

public enum Elements {
    None,
    Earth,
    Fire,
    Water,
    Wind
}

public enum HazardType {
    Bog,
    Pillar
}